/*
 * Ambient declarations for the root config files, which are type-checked via
 * `checkJs` in tsconfig.json.
 */

// eslint-config-rango ships no type declarations.
declare module 'eslint-config-rango';
